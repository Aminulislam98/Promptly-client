export const metadata = {
  title: "Go Premium",
  description: "Unlock all private and premium AI prompts with a one-time $5 payment. Unlimited publishing and priority support included.",
};

export default function PaymentLayout({ children }) {
  return children;
}
