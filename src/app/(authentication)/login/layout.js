export const metadata = {
  title: "Sign In",
  description: "Sign in to your Promptly account to discover and share AI prompts.",
};

export default function LoginLayout({ children }) {
  return children;
}
