export const metadata = {
  title: "Sign In",
  description: "Sign in or create an account to start sharing and discovering AI prompts on Promptly.",
};

export default function AuthLayout({ children }) {
  return children;
}
