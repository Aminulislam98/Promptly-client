export const metadata = {
  title: "Create Account",
  description: "Create a free Promptly account to start sharing and discovering AI prompts.",
};

export default function RegisterLayout({ children }) {
  return children;
}
